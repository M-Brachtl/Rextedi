[package]
name = "rextedi"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

[dependencies]
crossterm = "0.29.0"
unicode-segmentation = "1.12.0"
